# 1. Database Connection
# The standard key is spring.data.mongodb.uri. If your app runs, this is fine.
spring.data.mongodb.uri=mongodb://localhost:27017/ntmi_ticketing_db

# 2. Server Port
server.port=8080

# 3. App Name
spring.application.name=NTMI_Ticketing_System

# 4. Error Handling (UPDATED KEY)
# This replaces the old 'server.error.include-message'
server.error.include-message=always
# Note: If 'server.error...' still shows yellow, try:
#spring.web.error.include-message=always